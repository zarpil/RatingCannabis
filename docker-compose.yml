version: '3.8'

services:
  trichai-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: trichai-web
    ports:
      - "5173:80"
    restart: unless-stopped
    env_file:
      - .env
